syntax = "proto3";

package libproto;

message Span {
  string name = 1;
  int64 start_time = 2;
  int64 end_time = 3;
}

message TraceBatch {
  repeated Span spans = 1;
}

service CollectorService {
  rpc SendTraces(TraceBatch) returns (Ack);
}
